import { AllowableNextStatusTransposePipe } from './allowable-next-status-transpose.pipe';

describe('AllowebaleNextStatusTransposePipe', () => {
  it('create an instance', () => {
    const pipe = new AllowableNextStatusTransposePipe();
    expect(pipe).toBeTruthy();
  });
});
