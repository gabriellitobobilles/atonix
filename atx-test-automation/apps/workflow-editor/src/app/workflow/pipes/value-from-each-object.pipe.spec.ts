import { ValueFromEachObjectPipe } from './value-from-each-object.pipe';

describe('ValueFromEachObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueFromEachObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
