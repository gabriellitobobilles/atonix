import { ActionTypeStringPipe } from './action-type-string.pipe';

describe('ActionTypeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionTypeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
