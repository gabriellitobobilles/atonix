import { ValueFromEachGroupPipe } from './value-from-each-group.pipe';

describe('ValueFromEachGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueFromEachGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
