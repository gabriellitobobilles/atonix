selenium-hub:
  image: selenium/hub:latest
  shm_size: "2g"
  environment:
    GRID_TIMEOUT: 0
  ports:
    - 4444:4444

# firefoxnode:
#   image: selenium/node-firefox
#   expose:
#     - 5900
#   links:
#     - seleniumhub:hub

chromenode:
  image: selenium/node-chrome-debug:latest
  volumes:
    - /dev/shm:/dev/shm
  ports:
    - 5900
  links:
    - selenium-hub:hub