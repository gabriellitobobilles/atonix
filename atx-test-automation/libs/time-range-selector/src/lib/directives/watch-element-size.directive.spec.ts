import { WatchElementSizeDirective } from './watch-element-size.directive';

describe('WatchElementSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new WatchElementSizeDirective();
    expect(directive).toBeTruthy();
  });
});
